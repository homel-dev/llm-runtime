k8s/networkpolicy.yml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: allow-runtime-consumers
  namespace: llm-runtime
  labels:
    app.kubernetes.io/name: allow-runtime-consumers
    app.kubernetes.io/part-of: llm-runtime

spec:

  podSelector: {}

  policyTypes:
    - Ingress

  ingress:
    - from:
        - namespaceSelector: {}

      ports:
        - protocol: TCP
          port: 8000
