apiVersion: v1
kind: Service
metadata:
  name: llm-openai-api-gateway-omniroute
  namespace: llm-runtime
  labels:
    app.kubernetes.io/name: omniroute
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: llm-runtime
spec:
  selector:
    app.kubernetes.io/name: omniroute
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
