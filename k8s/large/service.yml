# k8s/large/service.yml
apiVersion: v1
kind: Service

metadata:
  name: llm-large
  namespace: llm-runtime
  labels:
    app.kubernetes.io/name: llm-large
    app.kubernetes.io/component: inference
    app.kubernetes.io/part-of: llm-runtime
    llm.runtime/tier: large

spec:

  selector:
    app.kubernetes.io/name: llm-large

  ports:
    - name: http
      port: 8000
      targetPort: 8000
